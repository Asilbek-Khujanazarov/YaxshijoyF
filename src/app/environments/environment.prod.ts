import { Environment } from "../interfaces/environment.interface";

export const environment: Environment = {
  production: true,
  baseUrl: 'https://yaxshijoy-back-016d95ece8ad.herokuapp.com/api' // Production API URL'ingiz (masalan)
};