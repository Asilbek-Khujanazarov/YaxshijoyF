import { Environment } from "../interfaces/environment.interface";

export const environment: Environment = {
  production: true,
  baseUrl: 'https://yaxshi-joy.onrender.com/api' // Production API URL'ingiz (masalan)
};
